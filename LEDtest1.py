from ev3dev import *

if __name__ == '__main__':
    main()
        while(True)
            if(Button.left())
                Leds.set(LEFT, GREEN)
            else
                LEds.set(LEFT, RED)
            if(Button.right())
                Leds.set(RIGHT, GREEN)
            else
                LEds.set(RIGHT, RED)
